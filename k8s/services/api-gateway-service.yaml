apiVersion: v1
kind: Service
metadata:
  name: api-gateway
  namespace: techbank
spec:
  type: NodePort
  selector:
    app: api-gateway
  ports:
  - port: 8000
    targetPort: 8000
    protocol: TCP
    name: http
    nodePort: 30000
